package models

import org.anormcypher.Cypher
import scala.collection.mutable.ListBuffer

/**
 * Created by Jordan on 20/02/14.
 */
object Skills {

//  def getAllByEmployee(name :String): Seq[String] = {
//    val req = Cypher("START n=node(*) WHERE n:BAS AND n.name={name} RETURN n.skill as skills").on(("name",name))
//    val listTmp =  req.apply().map(row =>(row[String]("skills")))
//    listTmp.toString
//
//  }

}
